这是一个 `vue` 前端项目(可视化旅游景点推荐系统), 下面请你在这一个项目现有的基础上再开发一个用户信息页用于展示用户信息, 要求如下:

1. 原来的系统有 "景点列表" 和 "我的喜欢" 和 "我的足迹" 三个栏目, 此时需要加上一个 "我的信息" 这一个栏目来展示当前用户信息
2. 暂时只需要展示用户名、性别、城市信息(后续还要加上其他的信息), 如果是头像建议默认使用 `/src/assets/images/default_avatar.jpg`, 并且不允许修改
3. 此时用户信息不允许修改(之后可能会添加新的功能) 

4. 注意合理把 "我的信息" 栏目添加到导航栏中, 保持风格统一

同时你需要的信息如下:

同时你需要使用的信息如下:

1. 项目使用 `pinia` 进行状态管理, 如果要添加新的状态管理请添加在 `src/stores` 中
2. 项目路由管理使用 `vue-router`, 可以参考 `src/routers/index.js`, 请合理设计项目路由从而尽可能多地实现组件的复用
3. 项目异步请求使用 `axios`, 可以参考 `src/axios.js` (注意一定要使用 `Axios` 发送异步请求 !!!)
4. 后端接口文档可以参考 docs/可视化旅游推荐系统接受设计文档.md

注意只需要实现 展示用于历史浏览记录的功能!!!